initState
const gameReducer = (state, action) => {
    switch(action.type){
        case 'FETCH_GAMES' :
            return {
                ...state
            }
    }
}

export default gameReducer